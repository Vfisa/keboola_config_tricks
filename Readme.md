This is a collection of Keboola configs. You can use Keboola CLI to deploy them into your project.

Follow the CLI documentation, though it should be just:   
`kbc persist`   
`kbc diff` // just in case   
`kbc push`   